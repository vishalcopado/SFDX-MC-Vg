@isTest
private class VReplaceTest {
@IsTest
    private static void testgetCalculatedValue() {
    Integer result = VReplace.getCalculatedValue();
    // commit with 2 close folders :
		// kompaldev
	  // {!Environment.multipleVars}
		// kpaliwalDummyVariableUat
		// <!-- Multi-line check -->
   // {!Environment.multiLineKompal} 
    }
}